# Polymarket Bot — Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to git.

# ── Polymarket / CLOB credentials ──────────────────────────────────────────
# Your Polymarket API key (from the CLOB client documentation)
POLYMARKET_API_KEY=

# Your Polymarket API secret
POLYMARKET_API_SECRET=

# Your Polymarket API passphrase
POLYMARKET_API_PASSPHRASE=

# ── Ethereum wallet ─────────────────────────────────────────────────────────
# Private key for the wallet used to sign orders (hex, without 0x prefix)
# WARNING: keep this secret — never commit or share it
PRIVATE_KEY=

# Ethereum RPC endpoint (e.g., Alchemy, Infura, or local node)
RPC_URL=

# ── Database ────────────────────────────────────────────────────────────────
# Path to the SQLite database file (default: ./market-data.db)
DB_PATH=./market-data.db

# ── Collection settings ─────────────────────────────────────────────────────
# Interval between data collection runs in milliseconds (default: 60000)
COLLECTION_INTERVAL_MS=60000

# Delay between per-market API calls in milliseconds (default: 200)
RATE_LIMIT_DELAY_MS=200
